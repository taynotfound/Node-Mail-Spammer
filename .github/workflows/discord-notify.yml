on:
  workflow_dispatch:
    inputs:
      reason:
        description: "This repo is archived/inactive — workflow disabled"
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "disabled"
